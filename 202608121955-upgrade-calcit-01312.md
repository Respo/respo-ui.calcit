# 升级 Calcit 与模块依赖

- 基于包含 `read-field` nil-safe 修复的最新 main，升级 Calcit 与 `@calcit/procs` 到 0.13.12。
- 同步 respo-router 0.8.4 与 respo-markdown 0.4.22。
- 通过 `cr --check-only` 验证 UI 入口与 reload 流程；Dynamic 仅作为现有框架边界债务记录。
