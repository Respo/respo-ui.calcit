# Align the reviewed provider chain / 对齐已审查的 provider 链

UTC: 2026-09-05T11:53:00Z

- Consume the immutable Respo 0.16.93 and Respo Router 0.8.11 releases so strict consumers receive the completed render/effect FFI boundary fixes.
- 消费不可变的 Respo 0.16.93 与 Respo Router 0.8.11 发布，使严格消费者获得已完成的 render/effect FFI 边界修复。
- This implementation change updates only the dependency graph; the Respo UI package version remains 0.7.16 until a separate metadata-only release from verified `main`.
- 本次实现改动只更新依赖图；Respo UI 包版本保持 0.7.16，待已验证的 `main` 上另行执行纯元数据发布。
