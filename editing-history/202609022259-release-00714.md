# Release 0.7.14

- Upgrade Calcit and `@calcit/procs` to 0.13.75.
- Align Respo to 0.16.89, respo-router to 0.8.8, and js-ffi to 0.1.11 for a warning-free strict dependency graph.
- Preserve nominal Store updates, normalize optional style maps, make route-index fallback explicit, and correct layout helper schemas from Component to Element.
- 将 Calcit 与 `@calcit/procs` 升级到 0.13.75，并将 Respo、respo-router、js-ffi 分别对齐到 0.16.89、0.8.8、0.1.11。
- 保持 nominal Store 更新，显式规范化可选 style map 与 route index fallback，并将 layout helper schema 从 Component 修正为 Element。
